services:
  go-cli:
    image: sunshine55golang:latest
    build: .
    container_name: go-cli
    command: tail -f /dev/null
    ports:
      - "8080:8080"
    volumes:
      - ./:/workspace:delegated